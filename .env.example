# Shared
NODE_ENV=development

# API
PORT=8080
API_PORT=8080
# API 단일 포트로 실행할 때는 PORT만 사용합니다.
OMX_STATE_ROOT=.omx/state/jobs

# Optional host port overrides for docker compose
# If REDIS_URL/REDIS_PORT are omitted, worker/API uses file queue under OMX_STATE_ROOT/.queue

# Worker
WORKER_CONCURRENCY=2
WORK_ROOT=/tmp/omx-web-runs
TMUX_KEEP_SESSION_ON_FINISH=1
JOB_SKIP_GIT_CLONE=0
JOB_CLI_BIN=codex
JOB_CODEX_CLI_BIN=codex
JOB_CLAUDE_CLI_BIN=claude
JOB_GEMINI_CLI_BIN=gemini

# CLI 인증/설정 홈 경로
# 비워두면 스크립트 기본값(`~/.codex`, `~/.claude`, `~/.gemini`)을 사용합니다.
# DEV_CREW_CODEX_HOME=
# DEV_CREW_CLAUDE_HOME=
# DEV_CREW_GEMINI_HOME=
# DEV_CREW_SHARED_AGENTS_DIR=/path/to/repo/config/cli/agents
# DEV_CREW_SHARED_SKILLS_DIR=/path/to/repo/config/cli/skills

# Optional per-role command templates (global defaults)
# JOB_PLANNER_CMD=codex exec --json --full-auto --skip-git-repo-check --cd "$JOB_WORKDIR" "planner role for task: $JOB_TASK"
# JOB_RESEARCHER_CMD=codex exec --json --full-auto --skip-git-repo-check --cd "$JOB_WORKDIR" "research role for task: $JOB_TASK"
# JOB_DESIGNER_CMD=codex exec --json --full-auto --skip-git-repo-check --cd "$JOB_WORKDIR" "designer role for task: $JOB_TASK"
# JOB_DEVELOPER_CMD=codex exec --json --full-auto --skip-git-repo-check --cd "$JOB_WORKDIR" "developer role for task: $JOB_TASK"
# JOB_EXECUTOR_CMD=codex exec --json --full-auto --skip-git-repo-check --cd "$JOB_WORKDIR" "executor role for task: $JOB_TASK"
# JOB_VERIFIER_CMD=codex exec --json --full-auto --skip-git-repo-check --cd "$JOB_WORKDIR" "verifier role for task: $JOB_TASK"

# codex/gemini만 설치되어 있을 때 팀 역할별 기본 명령 템플릿 (provider codex 기준)
# JOB_CODEX_PLANNER_CMD=codex exec --json --full-auto --skip-git-repo-check --cd "$JOB_WORKDIR" "plan: {TASK}"
# JOB_CODEX_RESEARCHER_CMD=codex exec --json --full-auto --skip-git-repo-check --cd "$JOB_WORKDIR" "research: {TASK}"
# JOB_CODEX_DESIGNER_CMD=codex exec --json --full-auto --skip-git-repo-check --cd "$JOB_WORKDIR" "design: {TASK}"
# JOB_CODEX_DEVELOPER_CMD=codex exec --json --full-auto --skip-git-repo-check --cd "$JOB_WORKDIR" "implement: {TASK}"
# JOB_CODEX_EXECUTOR_CMD=codex exec --json --full-auto --skip-git-repo-check --cd "$JOB_WORKDIR" "execute tests for: {TASK}"
# JOB_CODEX_VERIFIER_CMD=codex exec --json --full-auto --skip-git-repo-check --cd "$JOB_WORKDIR" "verify: {TASK}"
# JOB_GEMINI_PLANNER_CMD=gemini "plan: {TASK}"
# JOB_GEMINI_RESEARCHER_CMD=gemini "research: {TASK}"
# JOB_GEMINI_DESIGNER_CMD=gemini "design: {TASK}"
# JOB_GEMINI_DEVELOPER_CMD=gemini "implement: {TASK}"
# JOB_GEMINI_EXECUTOR_CMD=gemini "execute tests for: {TASK}"
# JOB_GEMINI_VERIFIER_CMD=gemini "verify: {TASK}"
